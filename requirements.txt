# Gasolio Tracker - Dipendenze per build eseguibile Windows
# Core web framework
Flask>=2.3.0
requests>=2.28.0

# Data retrieval
investpy>=1.0.8
pandas>=1.5.0

# Firebase authentication
firebase-admin>=6.0.0

# Build requirements
pyinstaller>=5.0.0

# Optional: Remove selenium and webdriver dependencies since they're commented out in the main code
# selenium>=4.0.0
# webdriver-manager>=3.0.0

# Remove problematic dependencies that are hard to package:
# blpapi - Bloomberg API (commercial license required)
# xbbg - Alternative Bloomberg connector
# forex-python>=1.8 - Not used in current code
# yfinance>=0.2.18 - Not used in current code
# tabulate>=0.9.0 - Not used in current code
# swiftshadow - Unknown/potentially problematic dependency
